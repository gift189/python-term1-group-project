# python-term1-group-project
This Python program calculates a student's grade based on 5 input marks. Gets 5 marks: Validates input (numbers only, 0-100 range). Calculates average and Determinesgrade(A, B, C, D/Fail).Prints average and grade.
